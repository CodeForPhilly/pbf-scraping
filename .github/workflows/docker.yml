on: [push]

jobs:
  build:
    name: Docker Docket Download Dude
    # This job runs on Linux
    runs-on: ubuntu-18.04
    steps:
      - name: Boot docker 
        uses: CodeForPhilly/pbf-scraping@v1
      - name: Install dependencies
        run: |
          python3 analyses/full_dockets/download.py
